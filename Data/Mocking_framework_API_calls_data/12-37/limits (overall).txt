
1. some projects have some conflicts with und commands(bigtop, cannot find the testfile even though und metrics says it has it)

2. some projects use deprecated classes or methods that leads to compilation errors and cannot use AST parser (any23)

3. AST parser cannot go through every single method invoked (archiva, mockasset.java)
















projects cannot be imported: beam cassandra click 

question mark: carbondata

 